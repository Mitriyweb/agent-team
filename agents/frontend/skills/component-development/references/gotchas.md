# Frontend Gotchas

- **Z-Index**: Avoid magic numbers; use a centralized z-index map.
- **Responsiveness**: Always test on multiple screen sizes (mobile, tablet, desktop).
- **Accessibility**: Never use `div` for buttons; use the `button` element.
- **CSS Approaches**: Read the `CSS_APPROACH` environment variable before starting.
